public class Instance {
	private String title;

	// Sets up the new node Instance with its title.
	public Instance(String newTitle) {
		title = newTitle;
	}

	// Returns this node Instance title as a string.
	public String toString() {
		return title;
	}
}
